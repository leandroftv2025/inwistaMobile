# ====================================
# INWISTAMOBILE - ENVIRONMENT VARIABLES
# ====================================

# ----------------------------------------
# SERVER CONFIGURATION
# ----------------------------------------

# Porta do servidor (padrão: 5000)
# Usado pelo Express para escutar conexões HTTP
PORT=5000

# Ambiente de execução (development | production | test)
# Afeta logs, otimizações e comportamento do servidor
NODE_ENV=development

# ----------------------------------------
# DATABASE CONFIGURATION (PostgreSQL)
# ----------------------------------------

# URL de conexão do PostgreSQL
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# OBRIGATÓRIO para produção (atualmente usando in-memory storage)
# Exemplo: postgresql://inwista:senha_segura@localhost:5432/inwistamobile
DATABASE_URL=postgresql://user:password@localhost:5432/inwistamobile

# Pool de conexões do banco (opcional)
# DATABASE_POOL_MIN=2
# DATABASE_POOL_MAX=10

# ----------------------------------------
# SESSION & AUTHENTICATION
# ----------------------------------------

# Secret para sessões Express (OBRIGATÓRIO em produção)
# Gere com: openssl rand -base64 32
SESSION_SECRET=change-this-to-a-random-secret-in-production

# Tempo de expiração da sessão (em milissegundos)
# Padrão: 24 horas
SESSION_MAX_AGE=86400000

# ----------------------------------------
# SECURITY
# ----------------------------------------

# Origem permitida para CORS (em produção, defina o domínio exato)
# CORS_ORIGIN=https://seu-dominio.com

# Rate limiting (requisições por minuto por IP)
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX_REQUESTS=100

# ----------------------------------------
# REPLIT INTEGRATION (opcional)
# ----------------------------------------
# Estas variáveis são configuradas automaticamente pelo Replit
# Não é necessário configurá-las localmente

# REPL_ID=
# REPL_IDENTITY=
# WEB_REPL_RENEWAL=
# REPLIT_CONNECTORS_HOSTNAME=

# ----------------------------------------
# LOGGING & MONITORING
# ----------------------------------------

# Nível de log (error | warn | info | debug)
LOG_LEVEL=info

# Formato de log (json | pretty)
LOG_FORMAT=pretty

# ----------------------------------------
# FEATURES FLAGS
# ----------------------------------------

# Habilitar logs de debug para desenvolvimento
DEBUG=false

# Habilitar WebSocket (atualmente implementado mas não usado)
ENABLE_WEBSOCKET=false

# ----------------------------------------
# EXTERNAL SERVICES (futuro)
# ----------------------------------------

# API de PIX externa (quando integrar com banco real)
# PIX_API_URL=
# PIX_API_KEY=

# Serviço de cotação de stablecoin (quando integrar)
# STABLECOIN_API_URL=
# STABLECOIN_API_KEY=

# ----------------------------------------
# NOTAS IMPORTANTES
# ----------------------------------------
# 1. NUNCA commite o arquivo .env no Git
# 2. Mantenha DATABASE_URL segura e rotacione credenciais regularmente
# 3. Use SESSION_SECRET forte em produção (mínimo 32 caracteres aleatórios)
# 4. Para desenvolvimento local, copie este arquivo como .env:
#    cp .env.example .env
# 5. Para produção, use variáveis de ambiente do servidor/container
